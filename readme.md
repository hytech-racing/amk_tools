`signal_type`: 2 = special signal, 0 = sercos parameter
`sig_attr`: 
    
- 0 = without control signal, 1 = with control signal
- 0 = unsigned (e.g. status word) , 2 = signed (e.g. actual torque value)
- 0 = higher-value bits are lost, 4 = signal is limited to the maximum valuet // Relative  'length in bits'